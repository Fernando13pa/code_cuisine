# n8n workflows

Pega en esta carpeta los workflows descargados desde n8n en formato JSON.

Ejemplo:

```text
generate-recipes.json
```

Antes de subirlos a GitHub, comprueba que no contienen API keys, tokens ni otros secretos escritos directamente en los nodos.
